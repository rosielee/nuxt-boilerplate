# nuxt-boilerplate

### To start dev server locally:

  ```npm run dev```

  ### To build & start for production:
  
  for static:
    
    npm run generate
    
  for server(node)
    
    npm run build
    
    npm run start
